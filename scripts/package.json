{
  "name": "concrete-docs-preprocessor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "preprocess": "node preprocess-docs.mjs",
    "preview": "node preview.mjs --serve",
    "preview:build": "node preview.mjs"
  },
  "dependencies": {
    "cron-parser": "^5.5.0",
    "js-yaml": "^4.1.0"
  }
}
