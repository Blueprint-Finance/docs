---
title: "frxUSD+ — Withdrawing"
sidebarTitle: "Withdrawing"
---

## Epoch lifecycle


This vault's cutoff runs on the cron schedule
`0 4 12 * * 1`. Requests submitted after the cutoff
roll into the next epoch instead. Closed-pending requests settle on-chain
and become claimable on the cron schedule
`0 4 12 * * 2`, which also starts the next epoch.

See [Withdrawal epoch lifecycle](/concepts/earn/withdrawals/epoch-cycle-concept#withdrawal-epoch-lifecycle) for the full
explanation of how epochs start, close, and settle.

## Queue position numbering



This vault has no cap configured, so every request queues into its
native epoch and settles in FIFO order within that epoch. See
[Queue position](/concepts/earn/withdrawals/queue-position-concept#queue-position) for the full mechanics.
