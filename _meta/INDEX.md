# Business knowledge base — index

Generated by the bootstrap pass (2026-05-14). Each entry: `id` · title · hook.

## public/earn

### vaults

- `earn.vaults.deprecation` · **Vault deprecation** — soft-closing a vault to new capital while keeping it reachable for holders.
- `earn.vaults.tvl-exclusion` · **TVL exclusion** — vaults that are not counted in aggregate TVL figures.
- `earn.vaults.balance-display` · **Simple balance display** — single-asset balance framing for grouped vaults.
- `earn.vaults.permissioned-access` · **Permissioned vault access** — allowlist-gated vaults and the restricted-access UI.
- `earn.vaults.multi-address` · **Multi-address vaults** — one vault product spanning multiple contract deployments.
- `earn.vaults.dynamic-apy` · **Dynamic APY display** — CMS-pinned vs live-computed headline APY and historical chart.
- `earn.vaults.yield-tab` · **Yield tab** — when a dedicated Yield tab is rendered on the vault page.
- `earn.vaults.versions` · **Vault versions (v1 and v2)** — synchronous v1 vs asynchronous v2 and why most rules describe v2.
- `earn.vaults.address-whitelist` · **Vault rendering controls** — production allowlist plus the `hiddenCard` and group `hidden` visibility flags that gate the Earn surface.

### deposits

- `earn.deposits.disable` · **Deposit disable** — closing the vault to new deposits without affecting holders.

### withdrawals

- `earn.withdrawals.disable` · **Withdrawal disable** — when withdrawals are suppressed (pre-deposit, migration, etc.).
- `earn.withdrawals.queue-and-delay` · **Withdrawal queue and delay** — async withdrawal semantics and the delay promise.
- `earn.withdrawals.epoch-cycle` · **Epoch lifecycle** — start, cutoff, payout, and epoch listing.
- `earn.withdrawals.caps` · **Withdrawal caps** — per-epoch cap UI and the gating flag.
- `earn.withdrawals.overflow` · **Withdrawal overflow** — slicing requests across epochs when demand exceeds cap.
- `earn.withdrawals.queue-position` · **Queue position numbering** — FIFO numbering and how overflow slices shift positions.
- `earn.withdrawals.cancellation` · **Cancelling a queued withdrawal** — when a request can be cancelled and what cancelling does.
- `earn.withdrawals.rc-config` · **Schedule changes (RC config)** — pre-announced cron transitions at epoch boundaries.

### rewards

- `earn.rewards.display` · **Rewards display** — CMS-authored reward badges on cards and vault pages.

### cross-chain

- `earn.cross-chain.bridge-eta` · **Bridge and withdrawal ETAs** — CMS-pinned ETAs for in-flight cross-chain steps.
- `earn.cross-chain.pre-deposit-claim` · **Pre-deposit share claiming** — claiming destination-chain shares via LayerZero.
- `earn.cross-chain.layerzero-messaging` · **Cross-chain messaging (LayerZero)** — the source → wait → destination pattern and what users pay for.

## public/sdk

- `sdk.overview` · **SDK overview** — what the SDK provides and what it is not.
- `sdk.versioning` · **SDK versioning and release tags** — `latest` vs `test` channels and vault-version coexistence.
- `sdk.integrations` · **Integration entry points** — choosing between core, react, and wagmi entry points.

## internal/nav

_(empty — out of scope for this bootstrap pass)_

## internal/enterprise

_(empty — out of scope for this bootstrap pass)_
